package Synchronized;

import static Synchronized.Print.getPrintln;
import static Synchronized.Process.process;

public class App {
    public static void main(String[] args) {
        process();
        getPrintln();
    }
}
